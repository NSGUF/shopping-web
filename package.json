{
  "name": "shopping-web",
  "version": "0.0.0",
  "scripts": {
    "server:install": "cd server && npm i",
    "web:install": "cd web && npm i",
    "install": "npm run server:install && npm run web:install",
    "server:dev": "cd server && npm run dev",
    "web:dev": "cd web && npm run dev",
    "dev": "npm run server:dev && npm run web:dev"
  }
}
